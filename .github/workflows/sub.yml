name: Git Submodules Init

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: chris-short/github-action-git-submodules@v0.1.0
